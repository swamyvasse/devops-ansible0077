- name: install mongodb
  hosts: mongodb
  become: yes
  tasks:
  - name: copy code from mongod.repo to mongodb
    ansible.builtin.copy:
     src: mongodb.repo
     dest: /etc/yum.repos.d/mongo.repo
  - name: install mongodb
    ansible.builtin.yum:
     name: mongodb-org
     state: installed